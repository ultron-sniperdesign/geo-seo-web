import { defineCollection, z } from "astro:content";

/**
 * Content collections — GEO IA (services, pillar/spokes, blog) doplním při
 * stavbě obsahu (Task 5+). Zatím prázdné = validní (nic zatím obsah nečte).
 */
export const collections = {};
